; Files d'attente (ACD). Démo "support" pour tester le routage entrant.
; Les membres (agents) seront ajoutés dynamiquement via AMI en phase 2.
[general]

[support]
strategy=rrmemory
timeout=20
retry=5
maxlen=0
announce-frequency=0
