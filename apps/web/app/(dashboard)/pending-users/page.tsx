import { PendingUsersClient } from "./PendingUsersClient";

export const metadata = { title: "Validation des inscriptions — SGI" };

export default function PendingUsersPage() {
  return <PendingUsersClient />;
}
